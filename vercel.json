{
  "buildCommand": "pnpm --filter @encrypter/crypto-core build && pnpm --filter @encrypter/web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
