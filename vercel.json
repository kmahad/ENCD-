{
  "buildCommand": "pnpm --filter @encrypter/crypto-core build && pnpm --filter @encrypter/web build && mkdir -p dist && cp -r apps/web/dist/* dist/",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
